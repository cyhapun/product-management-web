mixin subMenu(items, path)
  ul
    each item in items
      li
        a(href=`${path}/${item.slug}`)= item.title
        if item.children && item.children.length > 0
          +subMenu(item.children, path)