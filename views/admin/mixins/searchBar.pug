mixin search-bar(keyword) 
  form(id="form-search")
    .input-group
      input(
        type="text"
        placeholder="Input keyword"
        name="keyword"
        value=keyword
        class="form-control"
      )
      .input-group-append
        button(
          class="btn btn-success"
          type="submit"
        ) Find